
  # Create Prototype

A Vite + React project converted to JavaScript and JSX. It includes a modern dashboard-style UI with interactive charts, farm health monitoring, disease tracking, and admin views.

## Running the code

Run `npm install` to install the dependencies.

Run `npm run dev` to start the development server.
  